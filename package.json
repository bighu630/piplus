{
  "name": "piplus",
  "private": true,
  "type": "module",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bash scripts/dev.sh",
    "dev:api": "bun run apps/api/src/index.ts",
    "dev:web": "bun --cwd apps/web dev",
    "typecheck": "bun --cwd apps/api run typecheck && bun --cwd apps/web run typecheck && bun --cwd packages/domain run typecheck && bun --cwd packages/pi-client run typecheck && bun --cwd packages/shared run typecheck",
    "test": "bun --cwd apps/api test && bun --cwd apps/web test",
    "test:api": "bun --cwd apps/api test",
    "test:web": "bun --cwd apps/web test"
  },
  "devDependencies": {
    "@types/jsdom": "^28.0.3",
    "bun-types": "*",
    "happy-dom": "^20.9.0",
    "jsdom": "^29.1.1",
    "typescript": "*"
  }
}
